name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <4.0.0" # Updated to match your Dart SDK version

dependencies:
  flutter:
    sdk: flutter
  record: ^4.4.3
  just_audio: ^0.9.30
  path_provider: ^2.0.14 # Retained the higher version to avoid conflicts
  # Removed unnecessary audio dependencies:
  # - audioplayers: ^0.20.1
  # - flutter_sound: ^9.2.13

  # Retained other dependencies if they're used elsewhere in your project
  # If not needed, consider removing them as well
  # flutter_bloc: ^8.1.6
  # http: ^1.2.2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # If you have any assets, fonts, or other resources, declare them here
  # Example:
  # assets:
  #   - assets/images/
